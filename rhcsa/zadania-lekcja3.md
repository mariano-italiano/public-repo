1. Ułóż komendę (1 linia) która wyświetla nazwy wszystkich uzytkowników posiadających powłokę /bin/bash i dodatkowo zapisze wynik do pliku konta-bash.txt
2. Zatrzymaj ręcznie przez systemctl usługę ntp i ustaw aby nie była uruchamiana podczas startu systemu.
3. Uruchom dwa razy polecenie: cat /dev/zero > /dev/null & i za pomocą top sprawdź PID procesów. Zmień priorytet procesu na -5. Obserwuj zachowanie CPU.
